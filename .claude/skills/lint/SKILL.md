---
name: lint
description: Run Biome linter and show results
---

Run `npx biome check src/ tests/` and report issues.

If $ARGUMENTS is "fix", run `npx biome check --write src/ tests/` instead to auto-fix.
